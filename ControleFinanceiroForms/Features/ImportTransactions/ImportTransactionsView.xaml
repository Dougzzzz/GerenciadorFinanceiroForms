<UserControl x:Class="ControleFinanceiroForms.Features.ImportTransactions.ImportTransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControleFinanceiroForms.Features.ImportTransactions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#ffffff">
             
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Importar Transações" 
                       FontSize="36" FontWeight="Bold" Foreground="#101114" 
                       Margin="0,0,0,4" />
            <TextBlock Text="Selecione um arquivo CSV com as suas transações. Transações já importadas serão ignoradas." 
                       FontSize="16" Foreground="#686b82" 
                       TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <TextBlock Text="Categoria Padrão (Opcional)" FontSize="14" Foreground="#101114" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          DisplayMemberPath="Name"
                          Padding="8" FontSize="16" BorderBrush="#dedee5" BorderThickness="1"/>
            </StackPanel>
            
            <Button Grid.Column="1" Content="Selecionar Arquivo e Importar" VerticalAlignment="Bottom"
                    Command="{Binding ImportCommand}"
                    IsEnabled="{Binding IsImporting, Converter={x:Static local:InverseBooleanConverter.Instance}}"
                    Background="#7132f5" Foreground="#ffffff" 
                    FontSize="16" FontWeight="SemiBold" 
                    Padding="16,10" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="12"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" 
                   Foreground="#101114" FontSize="16" FontWeight="SemiBold" 
                   Margin="0,20,0,0" />

    </Grid>
</UserControl>
