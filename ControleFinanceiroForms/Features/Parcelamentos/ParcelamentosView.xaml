<UserControl x:Class="ControleFinanceiroForms.Features.Parcelamentos.ParcelamentosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControleFinanceiroForms.Features.Parcelamentos"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Estilo para os Cards de Resumo -->
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Template Customizado e Moderno para ProgressBar -->
        <Style TargetType="ProgressBar" x:Key="ModernProgressBarStyle">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#f4f4f6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Border Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="4"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Main Layout split in columns -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Acompanhamento de Parcelamentos" 
                       FontSize="36" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" 
                       Margin="0,0,0,4"/>
            <TextBlock Text="Cadastre compras ou dívidas parceladas e amortize o saldo com pagamentos parciais." 
                       FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </StackPanel>

        <!-- Conteúdo Principal dividido em duas colunas -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" /> <!-- Lista de Parcelamentos -->
                <ColumnDefinition Width="2*" /> <!-- Detalhes & Pagamentos -->
            </Grid.ColumnDefinitions>

            <!-- COLUNA ESQUERDA: Lista e Cadastro -->
            <Grid Grid.Column="0" Margin="0,0,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Form de Cadastro -->
                    <RowDefinition Height="Auto"/> <!-- Filtros -->
                    <RowDefinition Height="*"/>    <!-- DataGrid -->
                </Grid.RowDefinitions>

                <!-- Cadastro de Novo Parcelamento -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Novo Parcelamento" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1.2*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Descrição" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NewDescricao, UpdateSourceTrigger=PropertyChanged}" Padding="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                <TextBlock Text="Valor Total" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NewValorTotal, UpdateSourceTrigger=LostFocus}" Padding="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="0,0,8,0">
                                <TextBlock Text="Parcelas" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NewNumeroParcelas, UpdateSourceTrigger=PropertyChanged}" Padding="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="3" Margin="0,0,8,0">
                                <TextBlock Text="Data Início" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <DatePicker SelectedDate="{Binding NewDataInicio}" Padding="3" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <Button Grid.Column="4" Content="Adicionar" VerticalAlignment="Bottom"
                                    Command="{Binding AddParcelamentoCommand}"
                                    Background="{DynamicResource AccentBrush}" Foreground="#ffffff" FontWeight="SemiBold" 
                                    Padding="16,8" BorderThickness="0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Filtros e Mensagem de Erro -->
                <Grid Grid.Row="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="{Binding ErrorMessage}" Foreground="#e02020" FontSize="14" VerticalAlignment="Center"/>
                    <CheckBox Grid.Column="1" Content="Ocultar Quitados" IsChecked="{Binding FiltroSomenteEmAberto}" 
                              FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Cursor="Hand"/>
                </Grid>

                <!-- Tabela de Parcelamentos -->
                <DataGrid Grid.Row="2" ItemsSource="{Binding VisibleParcelamentos}" SelectedItem="{Binding SelectedParcelamento}" 
                          AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                          Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" RowBackground="{DynamicResource SurfaceBrush}" AlternatingRowBackground="{DynamicResource AppBackgroundBrush}" 
                          Foreground="{DynamicResource PrimaryTextBrush}"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource BorderBrush}" Cursor="Hand">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="2*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotal, StringFormat=C}" Width="1.2*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Restante" Binding="{Binding SaldoRestante, StringFormat=C}" Width="1.2*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Parcelas" Binding="{Binding FormattedParcelas}" Width="1.8*" IsReadOnly="True" />
                        
                        <!-- Coluna customizada de Progresso -->
                        <DataGridTemplateColumn Header="Progresso" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4,2">
                                        <TextBlock Text="{Binding FormattedProgress}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
                                        <ProgressBar Value="{Binding PercentualQuitado, Mode=OneWay}" Foreground="{DynamicResource AccentBrush}" Style="{StaticResource ModernProgressBarStyle}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Ações" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Excluir" Background="Transparent" Foreground="{DynamicResource AccentBrush}" BorderThickness="0" Cursor="Hand"
                                            Command="{Binding DataContext.DeleteParcelamentoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- COLUNA DIREITA: Detalhes do Parcelamento Selecionado e Pagamentos -->
            <Grid Grid.Column="1" Margin="16,0,0,0">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedParcelamento}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Novo Pagamento -->
                    <RowDefinition Height="*"/>    <!-- Histórico de Pagamentos -->
                </Grid.RowDefinitions>

                <!-- Registrar Novo Pagamento -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,8">
                            <TextBlock Text="Amortizar Saldo Devedor" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            <TextBlock Text="{Binding SelectedParcelamento.Descricao, StringFormat=Registrar pagamento para: \{0\}}" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                        </StackPanel>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1.2*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                                <TextBlock Text="Valor Pago" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NewValorPago, UpdateSourceTrigger=LostFocus}" Padding="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,8,8">
                                <TextBlock Text="Data Pagamento" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <DatePicker SelectedDate="{Binding NewDataPagamento}" Padding="3" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,8,0">
                                <TextBlock Text="Nota (Opcional)" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NewPagamentoNota, UpdateSourceTrigger=PropertyChanged}" Padding="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource SurfaceBrush}"/>
                            </StackPanel>

                            <Button Grid.Row="1" Grid.Column="2" Content="Pagar" VerticalAlignment="Bottom"
                                    Command="{Binding AddPagamentoCommand}"
                                    Background="{DynamicResource AccentBrush}" Foreground="#ffffff" FontWeight="SemiBold" 
                                    Padding="24,8" BorderThickness="0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Histórico de Pagamentos -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16,16,16,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Histórico de Amortizações" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedParcelamentoPagamentos}" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" CanUserDeleteRows="False" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" 
                                  RowBackground="{DynamicResource SurfaceBrush}" AlternatingRowBackground="{DynamicResource AppBackgroundBrush}" HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                  Foreground="{DynamicResource PrimaryTextBrush}" HorizontalGridLinesBrush="{DynamicResource BorderBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Data" Binding="{Binding DataPagamento, StringFormat=dd/MM/yyyy}" Width="100" IsReadOnly="True" />
                                <DataGridTextColumn Header="Valor" Binding="{Binding ValorPago, StringFormat=C}" Width="100" IsReadOnly="True" />
                                <DataGridTextColumn Header="Nota" Binding="{Binding Nota}" Width="*" IsReadOnly="True" />
                                
                                <DataGridTemplateColumn Header="Ações" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Excluir" Background="Transparent" Foreground="{DynamicResource AccentBrush}" BorderThickness="0" Cursor="Hand"
                                                    Command="{Binding DataContext.DeletePagamentoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
